# For local development — vite proxy handles /api routing, no VITE_API_URL needed
# The vite.config.js proxies /api to http://localhost:5001

# For production deployment (Vercel) — set this in Vercel dashboard:
# VITE_API_URL=https://your-backend.vercel.app/api
